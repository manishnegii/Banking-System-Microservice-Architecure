//package com.spring.account_service.client;
//
///**
// * Placeholder client for validating users via {@code user-service}.
// * <p>
// * For now we keep it local (no network calls) so the account-service can run standalone.
// * Later you can replace the implementation with Feign/WebClient to call user-service.
// */
//public interface UserValidationClient {
//    void validateUserExists(Long userId);
//}
//
